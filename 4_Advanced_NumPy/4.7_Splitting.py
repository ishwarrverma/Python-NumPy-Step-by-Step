"""
Divide an Array in sub parts

np.split() - for equally split
np.hsplit() - for horizontally split
np.vsplit() - for vertically split

"""

import numpy as np

arr = np.array([10,20,30,40,50,60])
print(np.split(arr,2))
